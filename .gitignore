# Local preview helpers (not part of the website)
.dev-server.ps1
preview.cmd

# Server-only secrets (created on the host, never committed)
config.php

# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# Throwaway admin CSS preview mock (not part of the app)
admin/_preview.html

# Composer / PHPUnit
/vendor/
composer.lock
